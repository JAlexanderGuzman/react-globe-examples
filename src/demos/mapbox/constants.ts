import type { MapStyle, MapProjection } from "./types";

export const DEFAULT_MAP_STYLE: MapStyle = "dark-v11";
export const DEFAULT_PROJECTION: MapProjection = "globe";
export const DEFAULT_CENTER: [number, number] = [0, 0];
export const DEFAULT_ZOOM = 2;
